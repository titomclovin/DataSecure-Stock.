/* style.css - CSS Puro sin frameworks externos (Evidencia del requerimiento) */

:root {
    --primary-color: #0d47a1;
    --secondary-color: #1976d2;
    --bg-color: #f4f6f8;
    --text-color: #333;
}

* {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
    font-family: Arial, sans-serif;
}

body {
    background-color: var(--bg-color);
    /* Uso de Flexbox para centrar contenido */
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
    color: var(--text-color);
}

.login-container {
    /* Uso de Grid para adaptabilidad */
    display: grid;
    place-items: center;
    width: 100%;
    padding: 1rem;
}

.login-box {
    background: white;
    padding: 2.5rem;
    border-radius: 8px;
    box-shadow: 0 4px 15px rgba(0,0,0,0.1);
    width: 100%;
    max-width: 400px;
}

.login-box header {
    text-align: center;
    margin-bottom: 2rem;
}

.login-box h1 {
    color: var(--primary-color);
    font-size: 1.5rem;
    margin-bottom: 0.5rem;
}

.login-box p {
    font-size: 0.9rem;
    color: #666;
}

.input-group {
    margin-bottom: 1.5rem;
    display: flex;
    flex-direction: column;
}

.input-group label {
    margin-bottom: 0.5rem;
    font-weight: bold;
    font-size: 0.9rem;
}

.input-group input {
    padding: 0.8rem;
    border: 1px solid #ccc;
    border-radius: 4px;
    font-size: 1rem;
    transition: border-color 0.3s;
}

.input-group input:focus {
    border-color: var(--primary-color);
    outline: none;
}

.btn-primary {
    width: 100%;
    padding: 1rem;
    background-color: var(--primary-color);
    color: white;
    border: none;
    border-radius: 4px;
    font-size: 1rem;
    cursor: pointer;
    font-weight: bold;
    transition: background 0.3s;
}

.btn-primary:hover {
    background-color: var(--secondary-color);
}

.login-box footer {
    margin-top: 1.5rem;
    text-align: center;
}

.login-box footer a {
    color: var(--secondary-color);
    text-decoration: none;
    font-size: 0.9rem;
}

/* Diseño Responsive para móviles */
@media (max-width: 480px) {
    .login-box {
        padding: 1.5rem;
    }
}